# 更新记录

## 0.10.49

- 修正公开发布锁文件的依赖版本，保证 GitHub CI 和从零安装依赖可复现；插件功能不变。

## 0.10.48

- 修复发送追问后历史生成图片因对话重绘而空白的问题。
- 区分历史上下文加载、文献读取、联网检索与思考状态；按问题读取限制单次证据量，全面阅读仍保留全文。
- 当前论文问题优先核查本地原文，缺失信息如实说明；明确本地片段引用不能混用网页行号。
- Qwen、Kimi、GLM、MiniMax 使用官方图标，MiniMax 套餐选项简化为 Token Plan。

## 0.10.47

- 添加 Qwen、Kimi、GLM、MiniMax API 连接入口及手动模型 ID，区分百炼 Token Plan 个人/团队版和 MiniMax 订阅 Key。
- 增加 MiniMax 原生联网检索、Kimi 国际端点；套餐搜索不回退到按量接口，实际搜索结果继续校验。
- 增加 Kimi、MiniMax 可用余额查询和 MiniMax 套餐 5 小时/周额度自动刷新；未公开的查询能力明确显示不可查询。

## 0.10.46

- 回答下方的“保存为笔记”与完整会话共用保存逻辑，保留本轮提问、回答、附图、生成图片、公式、表格及引用，不包含其他轮次。

## 0.10.45

- 历史会话右键菜单增加“保存完整会话为笔记”，按顺序保存全部提问、回答和引用；附图及生成图片保存为 Zotero 内嵌附件，独立于插件缓存。
- 单篇会话保存为文献子笔记，多篇会话关联原文献；没有关联文献的会话保存为个人文库独立笔记。

## 0.10.44

- 移除生图请求的关键词拦截，由模型理解生图、研究讨论与绘图代码等请求；向 API 和 Antigravity 连接明确说明当前不提供生图工具，保留实际图片能力校验。

## 0.10.43

- 移除联网检索、导入目标和继续分析的关键词分流，由当前模型在同一轮选择并组合可用工具，不增加意图分类请求。
- 阅读页按需调用本地论文工具，纯联网问题无需先解析 PDF；文献库同时提供本地分析和联网工具，保留来源、进度和失败校验。
- 明确要求导入后直接执行，保留本轮分类查询、真实论文核验、去重及实际导入回执。

## 0.10.42

- 防止上一轮异步保存覆盖下一轮的会话检查点；旧原生记录已丢失或清理失败时可继续重建、删除本地对话，清理失败报告原记录位置。
- ChatGPT、Antigravity 和 API 统一校验实际搜索完成，覆盖论文推荐和继续检索，整理已有结果或仅导入无需重复搜索。
- 保留 Responses 中间事件返回的搜索正文，避免最终事件缺少 output 时丢失内容，并去除增量与完成事件的重复文本。
- 为 Qwen3.5 / Qwen3.8 Omni 配置所需的 agent 搜索策略。

## 0.10.41

- 适度收紧模型连接与高级参数的表单间距、说明文字及账户组件区域，保留原有字号与布局。

## 0.10.40

- ChatGPT 与 Antigravity 账户随本地对话保存原生会话检查点，重启 Zotero 或重装插件后可恢复；恢复成功仅发送新问题。
- ChatGPT 联网检索也复用同一官方会话；恢复后重新绑定本轮工具权限。
- 模型、文献、压缩或历史变化时重建上下文；未完成的检查点不用于续接。不保存历史的会话及逐篇核查工作会话不启用跨重启恢复。


## 0.10.39 — 2026-09-22

- 对话回答署名仅显示产品图标与 INTHES，移除账户、模型和分隔点。
- 连接高级参数增加联网检索选项，接入百炼、火山方舟、智谱及 Kimi 官方 API 的搜索能力；校验实际搜索记录，失败时明确提示，继续使用现有论文核验和去重导入流程。
- Antigravity 提交授权码后等待登录确认，验证期间禁止重复提交，失败保留输入并显示错误。

## 0.10.38 — 2026-09-21

- Antigravity 改用退出指引和退出状态检查：官方 CLI 不支持后台 `/logout`，也不能通过 `-i` 执行该命令；在 CLI 中退出后才清理插件账户状态，取消正在进行的登录仍可直接操作。
- 将 CLI 模型查询的明确“未登录”返回识别为未登录状态，修复退出后再次登录被错误阻断的问题。

## 0.10.37 — 2026-09-21

- 修复模型用反引号包裹原文引用时显示为普通文字的问题；行内纯引用恢复页码按钮、悬浮预览和点击跳转，已有对话重新显示即可生效。
- 行内纯引用同步纳入来源校验、笔记和导出处理；真正的代码示例与代码块保持原样。

## 0.10.36 — 2026-09-21

- 修复实机复测发现的新窗口启动竞态：等待该窗口的文献列表初始化后，再注册选中事件；提前关闭窗口时取消等待。
- 在 Zotero 10.0.3 中验证审查修复、亮暗视觉排版、双窗口会话归属及两类账户的真实连续问答与压缩后重建。

## 0.10.35 — 2026-09-21

- 修复 0.10.34 无法安装：恢复 Zotero 安装器要求的 `applications.zotero.update_url`。本地测试版继续使用保留域名占位，通过 XPI 手动更新。
- 打包前校验 Zotero 必需的插件标识、更新地址和最高兼容版本，防止生成缺少必需字段的安装包。

## 0.10.34 — 2026-09-21

- 修复分类异步加载覆盖正在进行的对话；加载范围时暂停发送。同一历史会话在多个窗口打开时定位到已有面板，避免副本互相覆盖。
- 修复无图片文献库问答和引用纠正导致 ChatGPT / Antigravity 会话重建的问题，保留模型、证据、压缩和实际历史变化时的重建。
- 文献切片保留公式和代码，长表格按整行拆分并重复表头，合并行不被截断；超过预算的不可拆内容明确报错。新版切片使用独立指纹，防止旧分析与新来源错配。
- 直接阅读、分段提炼和汇总校验实际提供的引用；对话与笔记保留表格合并单元格及有序列表起始编号。
- 隐藏进度列表停止重绘，折叠结果按需渲染，可见条目仅更新变化部分；历史引用归属校验改用索引。
- 清理无效样式、旧按钮匹配和占位更新地址，精简重复提示；回收 Antigravity 启动失败目录。启动时回收 MinerU 旧结果包，当前 ZIP、Markdown 及其他 API 结果完整保留，运行中会话引用的旧图片留至下次启动。

## 0.10.33 — 2026-09-21

- 简化高级参数的图片输入选项，将「支持图片（已确认）」改为「支持图片」。

## 0.10.32 — 2026-09-21

- 同一 Antigravity 论文检索会话复用流式进程，连续追问无需每轮启动 CLI；同步插件追加的导入记录，避免显示历史与运行时历史不一致而重建。
- 每轮更新核验工具与导入权限；模型、历史及阅读模式变化时仍重建，中止或关闭时释放进程。
- 排查 Windows 闪窗：维持 Zotero 现有的无窗口启动方式；首次启动及独立额度查询的闪窗尚未可靠消除。

## 0.10.31 — 2026-09-20

- 移除上下文用量前的「≈」符号，直接显示已用 / 总上下文。

## 0.10.30 — 2026-09-20

- Antigravity 账户支持联网论文检索：按需调用原生搜索与网页读取，再由现有工具核验 DOI/arXiv、导入指定分类并去重，无需额外搜索 API Key。
- 修复 Antigravity 无法读取 MCP 参数定义时猜测工具参数的问题：直接提供工具定义，保留文件读写、命令及浏览器操作限制；检索和普通阅读使用独立的权限配置。
- 接入实时搜索进度；其他 API 连接继续提示不支持联网论文检索和导入。

## 0.10.29 — 2026-09-20

- 状态栏改为「占用圆环 · 已用/总上下文 | 5h / week 剩余额度 | 重置时间」，上下文数值采用 k / m 缩写；重置时间按额度窗口的相同顺序直接显示。
- 移除上下文与额度的悬浮说明和自动刷新提示文字，自动刷新行为保留。

## 0.10.28 — 2026-09-20

- 将「余额查询（可选）」移入连接编辑页的「高级参数」。
- 上下文剩余与账户额度／API 余额合并为输入框下方的单行状态栏，统一字号与对齐；详细 token 数、额度重置时间和查询说明通过悬浮查看，窄侧栏不再增加一行。

## 0.10.27 — 2026-09-20

- 账户额度约每分钟自动刷新，生成过程中仍继续更新；移除手动刷新按钮，ChatGPT 的 5 小时及周额度仅按服务实际返回的窗口显示。
- 输入框下方支持 API 可用余额：适配 DeepSeek 与 OpenRouter 官方接口；自定义连接可配置同源 GET 查询路径、金额字段和货币。OpenRouter 权限不足及未提供余额接口的服务会明确提示，不用 Key 调用限额代替账户余额。
- 余额查询合并并发请求，连接配置或 Key 变化后丢弃旧结果；查询失败保留上次金额并标记，缺失金额不显示为零。硅基流动已停用的旧接口不再尝试查询。

## 0.10.26 — 2026-09-20

- 新增 Antigravity 账户连接：自动检测官方 CLI、复用本机登录、获取模型、流式回答和推理强度；同一会话及同篇分段核查复用运行组件，文献库通过受限本机 MCP 调用现有文献工具。
- 输入框下方显示服务实际返回的账户剩余额度。ChatGPT 读取官方账户限额，Antigravity 按 Gemini 或 Claude/GPT 额度池显示；悬浮查看重置时间，支持手动刷新。缺失字段不补成 100%，查询失败保留并标明上次结果。
- Antigravity 使用隔离配置并禁用文件读写、命令和网页访问，不自动使用额外 AI credits；停止后释放进程与工具入口，会话释放后清理其临时目录。当前官方流式接口仅支持文字，图片输入、生图及联网论文检索仍需相应的其他连接。

## 0.10.25 — 2026-09-20

- 采用「页间」品牌图标：将选定的双页概念精修为 SVG，统一侧栏顶部、回答署名和插件管理器图标；侧栏图标随亮色与深色主题调整颜色。

## 0.10.24 — 2026-09-20

- 新增 ChatGPT 账户联网论文检索：使用官方组件的原生实时搜索，通过 Zotero 核验 DOI／arXiv 元数据，无需额外搜索 API Key 或预先选择 PDF。其他 API 连接明确提示不支持。
- 新增向现有分类导入论文：目标文库去重、已有条目复用、疑似重复保留、失败逐项说明；停止后保留已完成导入，回答中保存实际操作记录。
- 检索支持继续追问及历史恢复；普通文献阅读和核查不会启用联网搜索。

## 0.10.23 — 2026-09-20

- 修复 Zotero 宿主界面禁止选取文字导致问题、模型回答与逐篇分析结果无法局部选中的问题。支持拖选所需文字，并通过 Ctrl+C / Cmd+C 复制选区；输入框仍使用原有复制行为。

## 0.10.22 — 2026-09-20

- 修复模型将片段标记 `C` 误写为 `P`（如 `[S30P7P17]`）导致回答校验失败的问题。仅在完整对应的来源 `[S30P7C17]` 已提供时纠正，不补猜页码或片段号；对话、逐篇结果与导出保持一致。
- 强化文献库提示词，要求逐字复制来源标识。

## 0.10.21 — 2026-09-20

- 统一识别并核验中文括号、括号内空格及连续片段范围引用，修复已核查原文在综合回答中被误报为无效引用的问题；跨页范围仍逐一检查来源，不接受跨文献或缺失片段的引用。
- 继续任务时从未变更的文献缓存补齐已保存分析遗漏的引用，复用逐篇结果；引用恢复失败时保留分析文字并明确标记失败，不将其作为已核查证据返回模型。
- 文献库进度区区分逐篇核查与综合汇总状态，汇总失败时显示「重试汇总」。回答、逐篇结果和导出使用相同的引用识别规则。
- 汇总使用文献名称，提示模型不在面向用户的回答中展示 Zotero 内部数据库 ID。

## 0.10.20 — 2026-09-20

- 统一收紧工具栏、阅读范围、文献标题区、消息间距与模型连接栏的留白，增加可阅读区域。
- 同步调整连接与偏好、连接编辑、历史列表、文献进度及下拉菜单的间距；保留正文字号、1.5 倍行距与输入框尺寸调整。

## 0.10.19 — 2026-09-20

- 显示名称统一为 Inthes，移除中文品牌名；首页与关于页面的英文标语改为「Inthes. Where papers meet ideas.」，中文保留「文献与灵感之间」。
- 收紧偏好中的文献库阅读设置：减少上下留白、降低输入框高度，核查与 MinerU 并发仍左右并列。

## 0.10.18 — 2026-09-20

- 插件更名为「论间 Inthes」，同步更新工具栏、侧栏、回答署名、设置、笔记与导出文档中的名称。
- 保留英文标语「Between paper and idea.」，中文标语精简为「文献与灵感之间」。
- 安装包改为 `inthes-0.10.18.xpi`；插件标识、凭据和数据路径不变，可覆盖更新。

## 0.10.17 — 2026-09-20

- 清理没有入口的演示模式、旧历史操作、推理摘要回调、未使用参数和样式；笔记保存逻辑仅处理所选回答。
- 统一使用 MinerU 批量解析实现，删除独立串行读取及已移除的额度参数。
- 删除旧版历史迁移、Gemini 账户配置迁移、旧引用定位、Base64 文献图片和旧任务模型签名兼容；仅使用当前存储格式。
- 文献图片统一引用完整解析结果中的原始文件；保留当前会话图片保存、历史位置变更和缓存清理。

## 0.10.16 — 2026-09-20

- 移除图片预览右上角关闭按钮，只保留点击图片外空白处关闭；点击图片本身不关闭，也不切换尺寸。

## 0.10.15 — 2026-09-20

- 生成图片只保留点击放大预览，去掉尺寸切换按钮及再次点击图片缩放的行为。
- 关闭按钮直接移除预览层，不再依赖原生 `close` 事件完成清理；Esc、空白处关闭及返回图片焦点使用相同的清理逻辑。

## 0.10.14 — 2026-09-20

- 点击生成图片可打开窗口级放大预览，支持适应窗口与原始尺寸切换，兼容深浅色主题。
- 按 Esc、点击关闭按钮或预览空白处返回对话；生成过程中也可查看已生成的图片，保留复制与另存为操作。

## 0.10.13 — 2026-09-19

- 修复解析配置或附件变化后继续分析会清除已完成结果的问题；失效文献明确报错，不再进行无效的批量重试。
- 历史中个别图片缺失不再阻断文字保存、重命名、删除或迁移，并明确提示图片问题；保存失败不再覆盖原始生成错误。
- 修复账户组件异常后的进程回收、最终错误通知、忙碌线程释放和窗口关闭后的 RPC 超时；校验登录地址、参数、模型分页与实际只读权限。
- 流式回答识别非正常结束，停止时主动取消读取；修正工具图片移除后的说明、签名上下文预算和限额识别，隐藏代理凭据并限制错误信息长度。
- 修复重试后消息按钮错位、跨窗口删除后的失效操作、Word 非法字符与嵌套格式，以及 MinerU 压缩包条目、图片路径和限流处理。
- 审查逐项核实与未采纳原因见 `docs/REVIEW-2026-09-19-b-fixes.md`。

## 0.10.12 — 2026-09-19

- 阅读／文献库及对话／文献与进度标签使用平直下划线，去除选中状态的弧形。
- 文献范围标题只显示分类名称，不再追加“包含子分类”；分类包含范围保持原设置。
- “使用当前选中文献”移至“阅读范围”标题右侧，使用小图标与文字，悬浮时轻微高亮。

## 0.10.11 — 2026-09-19

- 同轮文献读取共享每篇最多 3 次重试的预算；重复工具请求不再重新上传、重试已耗尽预算的失败文献。
- 删除历史后回收无引用的 Folio 图片文件，保留其他历史及打开会话的共享图片。生成图成功落盘后释放对应内存字节。
- 恢复历史、切换模型或压缩后重建账户会话时，补回最近生成的图片；原账户线程复用时不重复上传。补回图片同样校验模型能力和请求容量。
- API 工具每轮计入文字、配图、工具定义和输出预留；容量不足时先移除较早的工具原图，明确保留文字证据，必要时再压缩。
- 保存笔记时明确提示生成图片未包含在笔记中，笔记内也保留此说明。图片仍可在原会话复制或另存。
- 流式错误或无效事件主动取消响应体，保留已显示内容和原始错误，不自动重放请求。

## 0.10.10 — 2026-09-19

- API 连接收到明确的生图请求时，在读取文献和发送请求前提示“不支持图片生成，请切换到 ChatGPT 账户”；保留草稿，不影响讨论图像生成研究或请求绘图代码。
- 为 API 对话补充图片输出能力说明，避免模型声称已经生成图片或给出虚构图片链接。ChatGPT 账户原生生图保持可用。
- 完成当前版本的代码审查，复现记录及待确认建议见 `docs/REVIEW-2026-09-19.md`；本次未顺带修改审查发现的其他行为。

## 0.10.9 — 2026-09-19

- ChatGPT 账户接收组件原生生成的 PNG 图片，修复纯图片回答被误判为没有文字；兼容仅在完成事件中返回的文字。
- 对话中展示生成图，支持复制图片和另存为 PNG；生成时显示等待状态，重复事件不会重复插图。
- 生成图以独立文件随对话记录保存，支持历史位置迁移与对话导出；不读取模型返回的任意本地文件路径。

## 0.10.8 — 2026-09-19

- 文献库阅读和逐篇核查在首轮全部结束后，对失败文献最多批量重试 3 轮；保留成功文献及已完成的分段证据，重试沿用并发与服务限流设置。
- 部分文献失败不再将整个分析标记为失败，最终回答依据成功文献并说明遗漏；重复工具调用不会在同一轮对话中重置分析重试次数，下一轮可继续原任务。
- 分组汇总失败同样批量重试；仍失败时明确返回原因和已完成的逐篇结果，不中断最终回答流程或声称综合了全部证据。

## 0.10.7 — 2026-09-19

- ChatGPT 账户组件启动时读取用户 Codex 目录 `.env` 中的 HTTP_PROXY、HTTPS_PROXY、ALL_PROXY 和 NO_PROXY，覆盖继承的同名代理变量；保留 Folio 独立的账户目录和登录状态。

## 0.10.6 — 2026-09-19

- 修复 Zotero 全文提取裁掉首尾空白页分页符时的页数校验失败：逐页确认空白位置后恢复页码，保留内部空白页和引用定位；不能确认的分页仍明确报错。
- 修复账户模式逐篇会话复用时，对此前已提供片段的合法引用被误拒绝；仍拒绝未读取的片段、其他文献及不存在的来源。
- 修复账户组件提前结束主回答时中止尚未完成的文献工具：等待已启动任务结束后，依据实际结果重新作答，不重复核查。
- 引用校验跳过代码中的来源格式示例，与正文的引用渲染规则一致；正文引用仍须有已提供的证据。

## 0.10.5 — 2026-09-19

- MinerU 提交按每分钟 50 个文件、结果查询按每分钟 1,000 次共享限流；429 统一冷却并有限重试，等待可取消。
- 移除 MinerU 额度查询、企业查询字段和提交前额度估算。
- 普通阅读与文献库阅读的来源片段统计改为每条回答末尾展示，随回答保存；移除顶部检索提示及“全面梳理请使用总结”文案。

## 0.10.4 — 2026-09-19

- 核查并发与 MinerU 并发输入框改为左右并列，在侧栏支持的宽度范围内保持同一行。

## 0.10.3 — 2026-09-19

- 阅读栏文案调整为「与 Folio 一起阅读」。核查并发改为整数输入，范围 1–8，默认 4。
- 新增 MinerU 并发整数输入，范围 1–50，默认 50；限制同时进入解析流程的文献数，完成后继续补入排队文献。仅启用 MinerU 时生效，关闭时批量使用 Zotero 本地解析。

## 0.10.2 — 2026-09-19

- 对话中输入「继续」「请继续分析」会按当前选中的任务 ID 续做，保留原问题、完整文献范围及已完成结果，与右侧「继续任务」使用同一恢复逻辑。
- 增加按任务 ID 恢复的模型工具；续做时禁止通过改写问题或缩小文献范围另建任务，避免重复分析与进度分裂。

## 0.10.1 — 2026-09-19

- 分离解析与核查流水线：MinerU 缓存未命中的 PDF 按每批最多 50 份提交，逐篇完成、逐篇核查；上传 3 路、结果下载及写入 2 路，避免大批结果堆积内存。
- 核查默认 4 路，设置允许 1–8 路，429 时降低后续并发。扩大普通论文单次提取预算，分组汇总也使用有界并发；保留已有任务的分段进度。
- 逐篇提取使用当前模型 Low 或其支持的快速档位，最终回答沿用用户推理设置。ChatGPT 每篇长文复用独立线程，并合并密集的历史保存操作。
- 新增解析前页数统计与企业额度查询；个人 Token 明确显示剩余额度不可查询，不把企业接口零值当作每日优先处理余额。

## 0.10.0 — 2026-09-19

- 侧栏新增「文献库」入口，可选分类、子分类及是否包含下级分类，或使用当前选中的文献；按主条目去重，最多 200 篇。直接阅读最多 5 篇，超过时提供转入文献库的入口。
- 同一个对话入口由模型按问题调用范围内的检索、原文阅读、逐篇分析与结果工具；选择分类不会上传 PDF，只有需要读取原文时才按当前设置调用 MinerU 或 Zotero，并复用有效缓存。
- 原文解析最多并发 3 篇，逐篇分析最多并发 2 篇；每篇独立记录解析与分析进度，支持暂停、继续和失败重试，完成结果随对话历史保留。
- 分段提炼与分层汇总保留原始引用，明确覆盖范围和失败文献；同问题、模型及附件的完成结果可复用，附件改变时保留原证据并要求新建会话。
- 文献工具接入 ChatGPT 账户、Chat Completions、Responses、Anthropic 与 Gemini API。模型须支持原生工具调用；文献库批量分析使用文字和图注，原图通过按需阅读工具提供给视觉模型。

## 0.9.2 — 2026-09-18

- 修复引用原文中的 HTML 表格被导出为标签文字、LaTeX 公式被重复转义的问题；保留 Markdown 中的原始表格和公式。
- PDF 和 Word 正确显示原文表格，Word 支持跨行、跨列的合并单元格，避免数据错列。
- PDF 替换文件遇到访问拒绝时，提示检查文件占用或更换文件名、保存位置。

## 0.9.1 — 2026-09-18

- 优化 Markdown、PDF 与 Word 的导出排版：按问题分组，弱化模型信息，统一回答标题层级与段落、列表间距。
- 引用改为简短数字编号，完整原文集中在末尾；Markdown 图片数据放到文末引用定义中，减少正文中的编码干扰。
- PDF 优化标题与正文、图片的分页；Word 明确字体与段落样式、关闭文字网格对行距的干扰，并改善表格留白和跨页表头。

## 0.9.0 — 2026-09-18

- 对话历史新增右键菜单与行末菜单，支持重命名、置顶、归档 / 取消归档、复制为 Markdown。
- 单条对话及全部已归档对话可导出为 Markdown、JSON、PDF 和 Word（.docx），保留引用、附图与公式。
- 已归档对话单独展示，支持确认后批量删除；当前在任一窗口打开的对话受到保护。
- 重命名、置顶与归档状态随本机历史保存，继续提问或更换保存位置后保留。

## 0.8.6 — 2026-09-18

- 回答字号调整为比原五号小一档的小五号（9pt），行距改为 1.5 倍，保留紧凑的段落与列表间距。

## 0.8.5 — 2026-09-18

- 回答正文改为小四号（12pt），适度收紧行距、段距、标题、列表、引用块、公式与代码块间距。

## 0.8.4 — 2026-09-18

- 已发送的用户消息按内容自适应宽度，右侧对齐，最大占对话区域的 85%；长文本自动换行，并随侧栏宽度调整。

## 0.8.3 — 2026-09-18

- 连接设置去掉“使用中”标记；点击连接条目进入编辑，实际使用的连接只在对话输入框上方切换。

## 0.8.2 — 2026-09-18

- MinerU 缓存保存完整返回 ZIP 及全部展开文件，包括 Markdown、JSON 和原图；旧解析缓存不再读取或迁移。
- 文献索引仅保存图片文件引用，选入请求后才读取和编码；保留返回原图，超大图片只在发送时缩放。
- 历史会话共享独立图片文件，保存位置变更时一起复制；清理解析缓存不影响已保存的历史与当前会话。
- 推理控件恢复紧邻模型名称，硅基流动已确认支持的模型提供高 / 最高与关闭选项，并支持其思考预算参数。

## 0.8.1 — 2026-09-18

- MinerU Token 仅保留 Zotero 自带的眼睛控件，移除重复的显示 / 隐藏文字。
- 偏好中使用稍粗的横线区分外观、文献解析、历史与缓存及关于分区。
- 修复更新插件时可能复用旧脚本、界面未同步更新的问题。

## 0.8.0 — 2026-09-18

- 偏好新增可选的 MinerU 官方云端解析，支持 Token、VLM / Pipeline、文档语言和 OCR；Token 存入 Zotero 凭据管理器。
- 按设置使用 MinerU 或 Zotero 解析；首页文献栏保持原样，解析失败明确提示并可重试。
- MinerU 结果保留正文、公式、表格、图片与原始页码；支持图片的模型可阅读文献配图，纯文字模型需明确选择仅阅读文字。
- 两种解析缓存分别保存；缓存位置、保存开关与清理入口共用，附件或解析参数变化后重新读取。
- 文献图片纳入上下文预算；ChatGPT 会话复用时不重复发送相同配图，压缩或更换上下文后重新提供证据。

## 0.7.2 — 2026-09-17

- 更新“隐私与数据”说明，明确本地凭据存储、服务认证与开发者之间的数据边界。
- 添加 CC BY-NC 4.0 版权声明、官方许可图标和协议链接；安装包附带许可文本。

## 0.7.1 — 2026-09-17

- 深色底色与 Zotero PDF 默认深色主题一致，统一输入框、菜单与文字配色。
- 文献栏统一使用“与Folio一起阅读”。
- 对话保存开关只影响之后新建的会话，不再清除旧记录；当前会话保持原保存方式。
- 更新对话记录相关文案，文献缓存增加独立开关，保留自定义保存位置及清理入口。

## 0.7.0 — 2026-09-17

- 按确认的阅读界面重做排版，加入亮色、深色和跟随系统，保存外观偏好。
- 连接与偏好分为两个选项卡；服务选择使用官方图标，历史与缓存位置按需展开。
- 统一模型、协议和推理强度浮层，模型列表支持搜索、能力说明与选中标记。
- 更新标语为 Between paper and idea.，品牌说明为 FOLIO · 专注阅读。
- 移除保存完整对话入口，保留单条回答存为笔记与粘贴图片。
- 保留三角形输入框拖动标志，上下文圆圈按真实服务用量逐步填充。

## 0.6.1 — 2026-09-17

- 适度收紧「连接与偏好」的分区、说明及表单间距，消除历史保存位置与文献缓存前的叠加留白；空状态提示不再占位。

## 0.6.0 — 2026-09-17

- 修复历史文件字段注入，校验配置与历史；服务错误按实际密钥脱敏，凭据更新失败时保留旧值，配置保存失败时恢复凭据。
- 普通 JSON 返回也识别未完成的回答；ChatGPT 连接增加退出登录，删除时退出并释放专用进程与会话。
- 设置与历史分别保存；旧默认历史迁移到 folio-history.json，并保留 state.legacy.json 作为迁移前备份。
- 缓存 PDF 提取文本，按附件路径、大小和修改时间失效；读取失败逐篇列出，支持重试或明确选择继续阅读已成功的文献。
- 设置增加缓存位置与清理按钮；清理只删除 Folio 缓存，不删除附件、历史或笔记。
- 记住每个窗口内各会话的滚动位置，保存侧栏宽度及输入框高度。
- 流式渲染复用完成的 Markdown 块与公式，按绘制开销调整刷新频率；用量显示仅在数据变化时重绘。
- 增加跨语言学术关键词检索和文献分布兜底；推理档位优先使用服务元数据及有限的官方能力规则。
- 读取 PDF、初始化账户及创建会话期间可以立即停止等待，迟到结果不会继续发送问题或更新界面。


## 0.5.2 — 2026-09-17

- 单篇文献数量和原文引用数量移至卡片顶部，与多篇文献布局一致。
- 移除输入框下方的常驻历史与笔记说明，减少底部空白；历史读取异常仍保留提示。

## 0.5.1 — 2026-09-17

- 上下文初始显示 100%，仅随新的服务端 token 用量更新；输入、生成中和缺失用量时保持上次显示。
- 移除推理摘要展示与主动请求，保留生成状态及推理强度选择；ChatGPT 收到推理事件后才显示思考中。
- 修复连接设置搜索模型后无法选中；当前对话不可删除，返回当前对话保留账户会话。
- 约束侧栏内容的固有宽度，避免页面切换改变侧栏尺寸。

## 0.5.0 — 2026-09-17

- 接入 Chat Completions、Responses、Anthropic、Gemini 与 ChatGPT 账户的服务端 token 用量；上下文每轮校正，缺失字段保留估算，输出预留单独计算。
- 自动读取可用的模型上下文窗口，设置中仍可填写更小的上限；旧的默认 200k 配置使用自动模式，其他自定义数值保留。
- 显示连接中、思考中（等待秒数）、回答中；服务提供的推理内容默认折叠，模型旁可直接选择推理强度。
- 同一 Folio 对话复用 Codex 会话，续问仅提交新增问题；切换模型、证据变化、历史压缩、取消或重试后重建，避免文献重复进入上下文。
- OpenRouter 两轮实际请求已验证输入、输出、缓存及推理 token 字段；ChatGPT 两轮已验证真实上下文报告与会话复用。

## 0.4.1 — 2026-09-17

- 首页与关于页标语改为「Between page and thought.」。

## 0.4.0 — 2026-09-17

- 历史设置支持选择文件夹或手动填写保存位置，留空恢复默认位置。
- 自定义目录仅保存对话历史；更改位置时保留并合并已有会话，同一会话采用较新的记录。
- 路径不可读或写入失败时显示错误并保护原有历史；旧的自定义历史文件保留。

## 0.3.1 — 2026-09-17

- 移除输入框左下角模型名称旁的展开符号，保留点击切换模型的功能。

## 0.3.0 — 2026-09-17

- 移除 Gemini 账户连接及其 CLI 检测、授权、模型列表与推理实现。
- 升级时移除已保存的 Gemini 账户连接，并修正当前连接选择；已有对话历史保留。
- Gemini API 与 ChatGPT 账户连接继续支持；不会卸载本机 Gemini CLI。

## 0.2.2 — 2026-09-17

- 修复 Windows 无法识别 npm 安装的 Gemini CLI：读取安装包入口并通过 Node 启动，支持无扩展名、CMD 与 PowerShell 启动脚本。
- 补充默认 npm 安装目录检测，保留手动启动参数与现有 Codex 连接方式。

## 0.2.1 — 2026-09-17

- 空白对话显示上下文剩余 100%，保留窗口容量提示。
- 首页与关于页标语改为「Room to think.」。
- 预置连接使用官方 SVG 标志，图标随插件离线加载；自定义连接保留字母标记。
- 多文献卡片将统计移至顶部，悬浮或键盘聚焦可查看完整文献列表；单篇显示不变。

## 0.2.0 — 2026-09-16

- 连接表单使用统一的下拉菜单；服务带简短说明，长模型列表可搜索，支持键盘选择。
- 首页使用「Understanding, page by page.」，内置 36 句有出处的阅读与学习名言，每次启动随机选择且不与上次重复。
- 更新三个提问入口，移除文献锁定、自动跟随选择和界面示例等面向开发的文案与入口。
- 空白对话显示「尚未开始」和窗口容量；开始输入后显示下次请求预算，提示说明包含回答预留，并非账户用量。
- 建立本地 Git 版本记录；版本显示与安装包名称读取 package.json，并在打包时检查插件清单版本。

## 0.1.10 — 2026-09-16

将本地已有、已验证的版本保存为首个 Git 基线。此前开发没有 Git 提交，以下为已有功能概要，不补造历史提交。

- 单篇问答、多篇总结、引用段落预览与 PDF 定位、本机历史及可选保存笔记。
- API 与账户连接、模型获取、连接切换排序、OpenRouter 指定提供商。
- 图片粘贴、模型能力提示、对话历史压缩、上下文预算。
- 流式增量显示、生成期间编辑草稿、侧栏与输入框尺寸调整。
- LaTeX 公式渲染与上下文计数修复。
